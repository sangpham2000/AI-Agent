# =============================================================================
# AI Agent - Environment Configuration
# Copy this file to .env and update the values
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Database
# -----------------------------------------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=ai_agent_db
POSTGRES_PORT=5432

# Flowise uses a separate database
FLOWISE_DB=flowise_db

# -----------------------------------------------------------------------------
# Flowise AI Orchestration
# -----------------------------------------------------------------------------
FLOWISE_PORT=3000
FLOWISE_USERNAME=admin
FLOWISE_PASSWORD=your_flowise_password_here
FLOWISE_API_KEY=

# -----------------------------------------------------------------------------
# Google Gemini Configuration
# Get your API key from: https://aistudio.google.com/apikey
# -----------------------------------------------------------------------------
GOOGLE_API_KEY=your_google_gemini_api_key_here

# Embedding Model Options:
# - text-embedding-004 (general purpose, 768 dimensions)
# - text-multilingual-embedding-002 (better for Vietnamese)
GEMINI_EMBEDDING_MODEL=text-embedding-004

# LLM Model Options:
# - gemini-2.0-flash (fast, recommended)
# - gemini-1.5-pro (more capable, slower)
GEMINI_LLM_MODEL=gemini-2.0-flash

# -----------------------------------------------------------------------------
# Telegram Bot Configuration
# -----------------------------------------------------------------------------
# Get this from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Webhook URL for receiving Telegram updates (optional, for production)
# Example: https://your-domain.com/api/telegram/webhook
TELEGRAM_WEBHOOK_URL=

# -----------------------------------------------------------------------------
# Core Service (Backend .NET)
# -----------------------------------------------------------------------------
CORE_SERVICE_PORT=5000

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-must-be-at-least-32-characters
JWT_ISSUER=ai-agent-api
JWT_AUDIENCE=ai-agent-clients

# -----------------------------------------------------------------------------
# Frontend Services
# -----------------------------------------------------------------------------
DASHBOARD_PORT=8080
PLUGIN_PORT=8081

# -----------------------------------------------------------------------------
# Keycloak SSO Configuration
# Run with: docker-compose -f docker-compose.keycloak.yml up -d
# -----------------------------------------------------------------------------
KEYCLOAK_PORT=8180
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin123
KEYCLOAK_DB_PASSWORD=keycloak123

# Keycloak Realm Configuration (after setup)
# KEYCLOAK_REALM=ai-agent
# KEYCLOAK_CLIENT_ID=ai-agent-app
# KEYCLOAK_CLIENT_SECRET=your-client-secret

# -----------------------------------------------------------------------------
# Optional: Additional Vector Database (if not using pgvector)
# -----------------------------------------------------------------------------
# QDRANT_HOST=qdrant
# QDRANT_PORT=6333
# QDRANT_API_KEY=

